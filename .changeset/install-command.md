---
"@cogmem/engram": minor
---

Add `engram install` command that installs the SKILL.md cognitive protocol and configures the MCP server for your AI editor. Supports `--provider`, `--global`/`--project` scope, `--dry-run`, and interactive prompts when flags are omitted.
